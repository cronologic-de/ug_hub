[project]
name = "ug-hub"
version = "0.1.0"
description = "docs.cronologic.de landing page"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "furo>=2025.12.19",
    "sphinx>=9.1.0",
]

[dependency-groups]
dev = [
    "esbonio>=2.1.0",
]
